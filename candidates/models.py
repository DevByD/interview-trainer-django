from django.db import models

# Candidate-specific models live here. The CandidateProfile model itself is
# defined in the ``accounts`` app because it is tied 1:1 to authentication.
# This app hosts candidate-facing pages (dashboard, profile).

# Reserved for Phase 2+: e.g. CandidateAssessmentInvitation views/helpers.
